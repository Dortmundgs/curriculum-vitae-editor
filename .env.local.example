# ── AI Writing Assistant ──────────────────────────────────────────────────────
# Optional: set your OpenAI API key here to enable server-side AI generation.
# If not set, users must enter their own key via the "AI Settings" toolbar panel.
# Get a key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# ── InstantDB (cloud save) ────────────────────────────────────────────────────
# Your InstantDB App ID — required for cloud save and login features.
NEXT_PUBLIC_INSTANTDB_APP_ID=your-app-id-here
